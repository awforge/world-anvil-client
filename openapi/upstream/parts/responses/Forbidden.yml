description: 'Access forbidden. The user is not allowed to access the resource.'
content:
  application/json:
    schema:
      type: object
      properties:
        success:
          type: boolean
          nullable: false
          default: false
        error:
          type: string
          nullable: false
          default: 'access_denied'